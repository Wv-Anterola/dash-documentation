# Local overrides. Apply with:
#   bundle exec jekyll serve --config _config.yml,_config_local.yml
#
# Uses the just-the-docs gem instead of remote_theme (no network round-trip)
# and serves from the site root so links work at http://localhost:4000/.
theme: just-the-docs
remote_theme:
baseurl: ""
url: ""
