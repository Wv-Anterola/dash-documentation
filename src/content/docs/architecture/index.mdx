---
title: Architecture
description: A source-verified map of Dash's document and field runtimes, rendering, collections, links, agents, synchronization, persistence, desktop packaging, and extension points.
---

import ProjectNote from '../../../components/ProjectNote.astro';
import SourceSnapshot from '../../../components/SourceSnapshot.astro';


This page describes the structure of the current system and the small number of
places you extend it. It is aimed at someone about to work on Dash, or someone
trying to judge what a project on the [Project index](/research/projects/)
page actually touched.

<SourceSnapshot />

## Architecture map

Read this page for the structural overview, then follow the subsystem that
matches the question you are trying to answer:

- [How Dash actually works](/architecture/engineering-model/) — the design
  pressures, state ownership rules, execution traces, and invariants that
  explain why the subsystems below exist.
- [Architecture decisions and tradeoffs](/architecture/decisions-tradeoffs/) —
  the pressure, mechanism, contract, cost, and reconsideration signal behind
  each major design choice.
- [System map](/architecture/system-map/) — client, server, persistence,
  external services, and desktop runtime at a glance.
- [Document model](/architecture/document-model/) — fields, prototypes,
  layout/data separation, delegation, and identity.
- [Field runtime and serialization](/architecture/field-runtime/) — typed
  values, parent/change wiring, all 21 serialized wire tags, lazy references,
  lists, casting, conversion, and hydration.
- [Rendering lifecycle](/architecture/rendering-lifecycle/) — factory to
  `DocumentView` dispatch to reactive view updates.
- [Collections and views](/architecture/collections-views/) — the collection
  render paths, layout engines, and view switching contract.
- [Links and trails](/architecture/links-trails/) — links as documents,
  anchors, backlinks, navigation, and presentation state.
- [Agents and AI](/architecture/agents-ai/) — retrieval, agent loops, tool
  execution, model adapters, and trust boundaries.
- [Server, storage, and security](/architecture/server-storage-security/) —
  Express routes, MongoDB, files, authentication, observed enforcement, and
  deployment boundaries. Use the
  [HTTP and service interface reference](/reference/http-service-interface/)
  for every registered method/path, observed input, response operation, and
  request-supervision branch, and the
  [synchronization protocol](/reference/synchronization-protocol/) for the exact
  Socket.IO event and consistency contract.
- [Import and export](/architecture/import-export/) — ingestion, conversion,
  external APIs, and portability limits.
- [Desktop and local models](/architecture/desktop-local-models/) — Electron
  packaging and the Ollama execution path.
- [Undo and provenance](/architecture/undo-provenance/) — transaction
  boundaries, reversibility, and experimental action-origin capture.

For implementation work, start with
[extension points](/development/extension-points/) and use the focused recipes
for adding a [document type](/development/add-document-type/), a
[collection view](/development/add-collection-view/), or an
[agent tool](/development/add-agent-tool/).

Keep the [runtime contract reference](/reference/runtime-contracts/) beside the
source when changing core document, rendering, synchronization, undo, link, or
agent paths. If a working system fails, use the symptom-first
[troubleshooting guide](/development/troubleshooting/) to isolate the boundary.


## The stack

TypeScript throughout, React and MobX on the client, Express and MongoDB on the
server, webpack for bundling. Since March 2026 there is also an Electron build
that packages the whole thing as a desktop application, and an agent service
that talks to a local Ollama instance, so the AI features can run against a
model on your own machine instead of a remote API. Ollama itself is not shipped
inside the installer; that step is on a branch.

The repository is a monorepo. The web application lives at the root; the shared
component library is a workspace under `packages/components` with its own
Storybook.

## One document abstraction

The idea the rest of the system depends on is that everything is a document.

A PDF is a document. So is a collection of PDFs. So is the link between two of
them, and so is a presentation trail, and so is a chat message. They differ by
their type and the fields they carry, not by being different kinds of object.

Documents are created from prototypes. Renderer modules populate the shared
prototype template map, and canonical factories in
`client/documents/Documents.ts` normally call `InstanceFromProto` with the
prototype fetched by `Prototypes.get(DocumentType.SOMETHING)`. System stores,
configuration records, and presentation resources use explicit special paths.
The [document type reference](/reference/document-types/) maps all of them.

This is why linking works the way it does. If a link only had to connect two
documents, it could be a row in a table. Because a link is itself a document, it
can carry a description, be annotated, appear in a collection, and be created by
an agent tool, all without special cases.

## Four recurring extension points

Most projects in the [Project index](/research/projects/) extend one of four
recurring mechanisms. They are not the whole architecture, but they explain a
large share of how independent projects became product capabilities.

### Document types

`client/documents/DocumentTypes.ts` declares the `DocumentType` enum. A complete
addition also needs a prototype registration, a construction path, a layout
component reachable from `DocumentContentsView`, persistence and permission
behavior, a product entry point, tests, and documentation.

There are fifty-one entries. They run from the core types (rich text, image,
web, PDF, collection, video, audio, ink) through to types added by recent
cohorts: the 3D viewer and sketch canvas, the data visualisation box, the video
generator, the life coach workspace and its events, policy testimony and policy
checker, journals, tasks, scrapbooks, and the agent itself.

This is a frequently used extension point: ten of the forty-two recorded
projects introduced document types. The centralized enum and startup registries
are also shared integration surfaces, so concurrent additions require explicit
coordination and full lifecycle verification.

<ProjectNote title="Document and field model">
  Supplies the prototype, data-document, and view-delegate ownership contracts
  used by every later type.
</ProjectNote>

### Collection view types

`CollectionViewType` decides how a collection displays what it holds. There are
twenty-one. Freeform, stacking, schema, tree, carousel, and docking come from
2019 and 2020. Calendar came from 2023. Graph came from 2026. Adding one means
an enum value and a view component; every existing collection can then be
switched into it without converting any data.

Layout engines for freeform collections are separate, under
`collectionFreeForm/collectionFreeFormLayoutEngines/`, so positions can be
computed rather than set by hand.

<ProjectNote title="Graph view for collections">
  The most recent addition here, and the one that exposed the cost of this extension point: a layout engine recomputing against MobX observables re-renders the whole collection every frame. The fix was to scale iterations by node count and stop once movement settles.
</ProjectNote>

### Agent tools

`client/views/nodes/chatbot/agentsystem/Agent.ts` constructs a registry of tools
the agent can call. A tool is a class with a schema and an execute method.
Twenty-two are registered at construction: searching documents, filtering and
sorting and tagging them, creating links, reading file contents, running a
calculation, scraping a page, driving the interface, generating an image,
setting a quiz, walking a user through a tutorial.

There is also a `tools/dynamic/` directory, because the agent can generate a new
tool and have it compiled into the running application.

This is the extension point that grew fastest. Most of the 2025 and 2026 agent
work is entries in this registry rather than new UI.

<ProjectNote title="Agent tool registry and dynamic tool creation">
  Built the registry itself, plus the dynamic path that compiles a generated tool into the running application. Everything below this line in the 2025 and 2026 cohorts depends on it.
</ProjectNote>

<ProjectNote title="Canvas awareness and UI control for the agent">
  A good example of how small an addition can be once the registry exists: two tool classes, one to enumerate canvas documents and one to drive the interface.
</ProjectNote>

### Undo batches

Mutations run through `util/UndoManager`, and `UndoManager.RunInBatch` groups a
set of changes under a name so they revert together. This matters more than it
sounds: it is what makes an agent action individually revertible.

It is also the only place in the system where "what the agent just did" is a
single addressable thing, which is why it is where provenance had to attach.

<ProjectNote title="Post-hoc oversight of prompt-injected agents">
  Wrapped the batch mechanism so each agent action carries the source content that caused it. Capture is off by default and the wrapper lives on a branch, so the shipped application still batches without recording origin.
</ProjectNote>

## Where the AI surfaces are

Dash exposes two AI surfaces with different effect boundaries.

The **chatbot** (`nodes/chatbot/`) is the retrieval-augmented surface. Documents
are chunked into a vector store, retrieved chunks go into the model's context,
and answers come back with citations that resolve to a source document and
navigate to it.

The **agent** (`nodes/agent/`) is the acting surface. It runs a ReAct-style loop
in `services/openaiService.ts` or `services/ollamaService.ts`, appending each
tool result back into the message stream, and its tools write to the workspace.

They share the tool registry. The distinction that matters is that one of them
answers and the other one edits.

:::note
Because retrieved content and tool results both enter the model's context as
messages, untrusted document content can carry instructions the model treats
as its own. That is the problem the [2026 oversight
study](/research/cohorts/2026/#post-hoc-oversight-of-prompt-injected-agents)
was built to measure. The provenance capture it added is off by default.
:::

## What this means for a new project

Identify the extension contract before implementation. A new document type, a
new collection view, or a new agent tool follows an established integration
path; a parallel subsystem next to one of those requires separate work across
the registries, lifecycle, persistence, and interface entry points. Many branch-only projects in the
research archive follow the second pattern.

## Where to read next

- [Concepts](/concepts/) for why the document and
  collection models are shaped this way, rather than only how.
- [Project index](/research/projects/) filtered by system area,
  which maps directly onto the extension points above.
- [Contributing to Dash](/contributing/joining/) if you are about
  to pick a project and want to know which slot it should aim for.
