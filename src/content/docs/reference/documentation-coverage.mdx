---
title: What this documentation does not know
description: "A generated report of its own gaps: for every traced surface, how many records carry a source line, how many carry an explanation written by a person, and exactly which ones are still missing one."
sidebar:
  order: 5
---

import CoverageReport from '../../../components/CoverageReport.astro';

<p class="lede">
  Every reference here mixes two kinds of claim, and only one of them can be
  generated. This page measures the other one and names what is missing.
</p>

## Two kinds of claim

A parsed claim is a signature, a file and a line, a call, a field write. It is
as true as the parser, it is checked on every build, and it breaks loudly when
the code moves. There is a lot of it here, and it is the part that lets you
verify anything on this site against the exact commit it came from.

A written claim is what the thing is for, what it means for you, and when not
to reach for it. Nothing generates that, nothing checks it, and it is the part
almost every reader actually needs. A documentation set can be complete in the
first sense and useless in the second, which is why the two are counted
separately below.

The measurement is deliberately shallow: a record counts as explained when its
plain-language field contains text. Whether the text is *good* is not something
a script can judge, and pretending otherwise would make the number worse than
no number at all.

There is a third count, kept apart from both. Some records have no explanation
of their own but do have a reviewed one a single link away: 41 scripting globals
are the function behind a documented interface control, and that control's
explanation was written and checked by a person. Those are reported as
**answerable through a cross-reference**, never folded into "explained", because
knowing which button calls a function tells you what one particular call does
rather than what the function is for. The figure that matters for planning work
is the third one: how many records have no explanation anywhere.

## The report

Ordered by the size of the gap, so the work queue is at the top.

<CoverageReport />

## Reading the two large gaps

**Scripting globals** are the functions Dash exposes to scripts you write
inside the application. Their explanation is the second argument to
`ScriptingGlobals.add`, which means it lives in Dash-Web and Dash shows it to
the person writing a script. A missing one is a gap in the product before it is
a gap here: the parsed calls and field writes below each entry are this site
reconstructing what the registration should have said.

It is worth saying why this gap did not close the way the HTTP one did. The
same generator move was tried: recover the comment above the registration. It
was then abandoned, because there are twelve such comments above 151
registrations and one of them is repeated above eight unrelated globals,
describing exactly one of them. Importing it would have put a confident, wrong
sentence on seven entries and raised this percentage while making the site less
true. The comments are recovered and audited instead, and the audit is
published alongside the dataset so the lines worth fixing are named. The
reasoning is set out in full under [why most rows have no
description](/guides/features/scripting/#why-most-rows-have-no-description).

**HTTP routes** used to score zero here, because the dataset had no
explanation field at all. It has one now, and the number moved without anyone
writing a word of new prose: roughly a third of the registrations in Dash-Web
already carried a comment saying what the route was for, and the generator now
recovers it instead of leaving it in the file. The rest genuinely have none.

That is deliberately a narrow measure. Every route also belongs to a family
whose purpose is explained on the [HTTP service
interface](/reference/http-service-interface/) page, and every route now
carries its recovered calls and the effects they imply. The family purpose is
written per file rather than per route, because a file with one job is a claim
a reader can check, while a hundred invented per-route sentences would be a
hundred chances to be confidently wrong about code nobody documented. Counting
the family purpose once per route would have moved this column to 100 percent
and meant nothing.

Neither gap is presented as finished. Both are more useful stated than hidden.

## Why publish this

The usual practice is to publish a coverage number only when it is high. That
makes the number a marketing artifact rather than a measurement, and it removes
the one signal that would tell a contributor where to start.

There is a second reason, specific to this site. Because so much here is
generated, it is easy to mistake volume for understanding: nine hundred traced
records look authoritative whether or not anyone has explained a single one.
Counting the explanations separately is what keeps that from being a claim this
documentation makes about itself without evidence.

## How this page stays honest

The report reads the published datasets directly rather than a hand-kept
tally. Generation fails when a dataset disappears, empties, stops carrying
source locations, or loses the field this report reads for its explanation.

It deliberately does **not** fail on low coverage. A gate that failed the build
when a number dropped would create pressure to write a sentence rather than to
write the right sentence, and the first thing to go would be the honesty of
this page. Non-regression floors live in the test suite instead, where changing
one is a visible edit somebody has to justify.

## Where to go next

- [Generated data endpoints](/reference/generated-data/) to read any of these
  datasets directly.
- [Scripting](/guides/features/scripting/) for the largest gap, with the parsed
  effects that stand in for the missing descriptions.
- [HTTP service interface](/reference/http-service-interface/) for the second.
- [Writing for this site](/contributing/documentation/) if you want to close
  one.
