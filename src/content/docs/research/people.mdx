---
title: People
description: Faculty, staff, and provisional student cohort rosters assembled from the surviving project record.
---

import { faculty, unresolvedContributors } from '../../../data/dash';
import AllCohortRosters from '../../../components/AllCohortRosters.astro';
import ResearchArchiveLinks from '../../../components/ResearchArchiveLinks.astro';

## Structured person records

These records keep verified identity and project participation separate from
code attribution. Commit authorship is never inferred to mean project
authorship.

<ResearchArchiveLinks kind="people" />

## Faculty and staff

These roles span every cohort rather than belonging to one.

<table class="ref-table">
  <thead>
    <tr><th scope="col">Name</th><th scope="col">Role</th></tr>
  </thead>
  <tbody>
    {faculty.map((f) => (
      <tr>
        <th scope="row">{f.name}</th>
        <td>{f.role}</td>
      </tr>
    ))}
  </tbody>
</table>

For questions about the project or about joining it, contact Andries van Dam at
[andries_van_dam@brown.edu](mailto:andries_van_dam@brown.edu).

## Cohorts

Rosters are provisional. They combine historical team pages, project records,
and repository authorship where a contributor identity can be verified. They
describe the surviving record and should not be treated as a complete statement
of research-group membership or project authorship.

<AllCohortRosters />

## Names we could not recover

These accounts appear in the 2019 and 2020 repository history under a handle or
first name only. Their contributions are retained in the record, but the
available evidence is insufficient to verify a full identity.

<ul class="roster">
  {unresolvedContributors.map((u) => <li>{u}</li>)}
</ul>

One more, recorded in the 2025 and 2026 history under the GitHub handle
`Skitty1238`, wrote the selection-aware text formatting work that is in
`master` today. If any of these are you, or you know who they are, please open
an issue on the documentation repository.

## Keeping this current

Rosters live in `src/data/dash.ts`, so adding next year's cohort is one edit in
one file. The [documentation guide](/contributing/documentation/) explains the
process.
