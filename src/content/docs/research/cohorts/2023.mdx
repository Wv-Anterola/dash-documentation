---
title: Spring 2023 to Fall 2023
description: First LLM features, the calendar and map document types, and the physics simulation platform that no longer exists.
---

import CohortProjects from '../../../../components/CohortProjects.astro';
import CohortRoster from '../../../../components/CohortRoster.astro';

Dash picked up its first LLM features and several new document types, and the
group started using it in CS1951V.

## What this year was about

Two new types from this year are still in daily use: the calendar collection
view and the Mapbox-backed map document, both built in a single burst in
December 2023.

The physics simulation platform is the other story from 2023, and it is a
different kind. It was a real system with three modes, it was documented on
this site, and it is not in Dash any more. We kept both the project entry and
the original documentation page, marked as removed. A research group that
deletes the record of work that did not survive ends up unable to explain its
own history, and the students who built it still built it.

This is also the year Dash was used in CS1951V, the hypertext course, which
produced the student use-case videos linked from
[what Dash is](/overview/what-dash-is/).

## Who was here

<CohortRoster cohort="2023" />

## Projects

Each entry opens with the problem it set out to solve. The approach, the
evidence behind its status label, and the source locations are one click below
it. Labels are defined in the
[status taxonomy](/reference/status-taxonomy/).

<CohortProjects cohort="2023" />
