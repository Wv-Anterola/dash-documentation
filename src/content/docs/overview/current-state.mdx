---
title: What Dash currently supports
description: The capabilities present in the current master branch, the boundaries of the implementation, and what the active cohort is working on.
---

import ProjectNote from '../../../components/ProjectNote.astro';
import SourceSnapshot from '../../../components/SourceSnapshot.astro';

This page summarizes the verified implementation. The complete feature-level
inventory is the [capability catalog](/capabilities/).

<SourceSnapshot />

## Counts

The inspected source contains 51 document-type enum entries, 21
collection-view enum entries, 19 collection render paths, 15 perspectives in
the ordinary appearance menu, and 22 agent tools registered at startup.

Enum counts include internal, special, loading, and sentinel types. See the
[document type](/reference/document-types/),
[collection view](/reference/collection-views/), and
[agent tool](/reference/agent-tools/) references for classified inventories.

## What works today

**Documents.** Rich text, images, web pages, PDFs, video, audio, recordings,
ink, and collections, all sharing one document implementation. Newer types
include 3D model viewing and sketching, data visualisation from CSVs, video
generation, policy analysis, life-coach workspaces, scrapbooks, journals,
tasks, and equations with a symbolic solver. Data visualisation now includes a
scatter plot and a semantic-map workflow that projects selected text, PDFs,
webpages, and images into a linked exploratory view.

**Collections.** Freeform canvas, schema table, stacking, notetaking, tree,
carousel, grid, masonry, pivot, calendar, and a link graph. Switching between
them converts nothing.

**Linking.** Links anchor to regions, selections, PDF pages, ink strokes, and
timestamps. A link is itself a document, so it carries its own description.

**Trails.** Prepared paths through a corpus that you can leave at any point,
with spring-based transitions and support for branching.

**AI.** Two surfaces. A retrieval-augmented chatbot that answers with citations
resolving back to source documents, and an agent with twenty-two tools that can
filter, sort, tag, link, and create documents.

**Desktop.** Electron builds for macOS and Windows, and an agent service that
talks to a local Ollama instance, so the AI features can run against a model on
your own machine instead of a remote API. You install Ollama separately; the
build does not yet ship it.

## Where the implementation stops

These are limits worth knowing before you plan work around them.

Document types cannot be defined outside the core enum, so every new type edits
the same two files and two cohorts adding types in one term will conflict.

Collection views do not compose. A calendar of stacks is not expressible.

Links have no type or strength, so a citation and a passing association are
indistinguishable except by a description someone remembered to write.

The agent has no permission model. A tool that can write can write to anything
the user can, and the agent decides when to call it.

Generation quality is not evaluated anywhere in the codebase.

## What the current cohort is doing

The [Spring 2026 to Fall 2026 cohort](/research/cohorts/2026/) added the link
graph view, policy analysis documents, a life-coach workspace, and scene-based
video generation, all of which are in `master`. It also produced work that is
deliberately not in `master`:

<ProjectNote title="Post-hoc oversight of prompt-injected agents">
  A study instrument rather than a feature. It measures whether a person
  reviewing an agent's edits actually catches one that came from a prompt
  injection. The provenance capture it adds is off by default.
</ProjectNote>

<ProjectNote title="Sketch-to-CAD drawing in 3D">
  Working on a branch: draw a shape on a plane in the 3D scene, recognise it,
  extrude it, split and dimension it, add hinges or engraving, and export STL.
  The joint branch was still active and unmerged on 27 July 2026.
</ProjectNote>

<ProjectNote title="AI-authored interactive HTML documents">
  An unmerged July 2026 branch adds prompt-driven patches to an HTML document,
  persistent embedded state, and a small bridge for reading local document
  metadata and links. It is implemented, but it is not a current product entry
  point.
</ProjectNote>

## Reading status labels

Every project on this site carries one of six labels, and the distinction that
matters is between *integrated* and everything else. Integrated means it can be
pointed at in the current `master` branch: a registered document type, an entry
in `CollectionViewType`, a tool in the agent registry, or code one of those
calls. A merged branch does not qualify, and neither does a demo recording or a
finished report.

The [status taxonomy](/reference/status-taxonomy/) defines all six labels and
the evidence each one requires. The
[implementation snapshot](/reference/implementation-snapshot/) records the
exact source revision behind this page.
