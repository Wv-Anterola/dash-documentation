---
import { verifiedAgainst } from '../data/capabilities';
import { withBase } from '../data/dash';
---

<details class="source-snapshot">
  <summary>Source scope</summary>
  <span>
    Technical claims reflect the Dash-Web source reviewed on
    <time datetime={verifiedAgainst.date}>{verifiedAgainst.date}</time>.
    Exact revision and verification methods are recorded in the
    <a href={withBase('/reference/implementation-snapshot/')}>source coverage note</a>.
  </span>
</details>
