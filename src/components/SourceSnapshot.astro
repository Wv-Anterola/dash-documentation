---
import { verifiedAgainst } from '../data/capabilities';
---

<aside class="source-snapshot">
  <strong>Implementation snapshot</strong>
  <span>
    Verified against <code>{verifiedAgainst.repository}</code>,
    <code>{verifiedAgainst.ref}</code> at
    <code>{verifiedAgainst.commit}</code> ({verifiedAgainst.date}).
  </span>
</aside>
